describe('test_special_operations', () => {
  beforeEach(() => {
    cy.visit(Cypress.env('BASE_URL'))
  })
  it('Must click on a number, on "x²", on "=" and check the result.', () => {
    cy.log('Ainda não feito')
  })
})
